# 芒果租车财务中台

这是芒果租车财务中台项目。当前代码只是整个中台的一部分，现阶段重点完成了账号登录、定金单/尾款单/对账单图片生成、历史图片、企业微信推送和基础账号管理。后续会逐步扩展为覆盖订单、车辆、客户、押金、收入、成本、报销、利润、审批和经营分析的完整财务业务系统。

## 当前能力

- 登录与账号权限
  - 支持账号密码登录。
  - 用户表为 `cw_user`。
  - 支持管理员和工作人员权限。
  - 只有管理员可进入账号管理和推送配置。

- 单据生成
  - 定金单：生成 WebP 图片，保存到 `create_file/djd/日期/`。
  - 尾款单：生成 WebP 图片，保存到 `create_file/wkd/日期/`。
  - 对账单：生成 WebP 图片，保存到 `create_file/dzd/日期/`。
  - 三类单据均基于 `skills/` 下的独立 skill 渲染。

- 历史图片
  - 按单据类型查看历史图片。
  - 每类图片按日期分组。
  - 支持预览、下载和推送。

- 企业微信推送
  - 支持配置企业微信 webhook。
  - 生成图片后可直接推送到企业微信群。
  - 推送时后端会临时将 WebP 转为 JPG，并按企业微信 image 消息要求生成 base64 和 md5。

- 数据库记录
  - 下载或推送当前生成的图片时，会把对应表单数据写入数据库。
  - 当前相关表包括：
    - `cw_user`
    - `cw_djd`
    - `cw_wkd`
    - `cw_dzd`

## 中台蓝图

`中台.xlsx` 是之前财务中台的表结构参考文件，里面包含大量子表。这个项目后续会以这些表为基础，逐步把 Excel/智能表格中的业务流程产品化。

目前从 `中台.xlsx` 中识别到的主要模块包括：

- 订单与结算
  - `订单结算主表（销售&车队长&财务）`
  - `定金单`
  - `收入对账表`
  - `客户资金流水表（财务）`
  - `押金管理表`

- 客户与销售
  - `客户主档案（销售）`
  - `业务员押金流水表`

- 车辆与车队
  - `车型参数表（车队长）`
  - `出车交付中心ing_1`
  - `在外车辆（包月）`
  - `长租车管理表`
  - `车辆异常ing（车队长）`
  - `回车违章情况(车队长)`
  - `车辆折旧台账`

- 维修、保险与配件
  - `车辆维修成本表`
  - `维修报备申请表`
  - `维修成本表(车队长&财务)`
  - `配件采购表（车队长&财务）`
  - `保险&续费记录（车队长）`

- 挂靠与合作车辆
  - `挂靠车主（车队长）`
  - `挂靠车主（车队长）_1`
  - `挂靠合作明细表`
  - `挂靠合作明细表(副本)`
  - `挂靠车辆成本利润表`

- 成本与费用
  - `自有车辆成本利润表`
  - `板车费用明细表（车队长）`
  - `GPS付款`
  - `代驾费用明细表（财务）`
  - `报销费用明细（财务）`
  - `调车成本表（车队长&财务）`
  - `运营杂费报销表（财务）`
  - `成本明细（财务）`
  - `费用汇总表（财务）`

- 财务报表与经营分析
  - `年度利润表（财务）`
  - `收入明细（财务）`
  - `税务凭证（财务）`
  - `工资报表`
  - `酒店付款台账`
  - `透视分析表`

- 审批与同步
  - `同行付款申请`
  - `报销`
  - `报销1`
  - `审批明细（示例）`

## 技术结构

- 前端：Vue 3 + Vite
- 后端：Node.js 原生 HTTP 服务
- 数据库：MySQL
- 图片渲染：本地 skill + Chrome/Chromium 截图 + WebP 转换
- 推送：企业微信 webhook

主要目录：

```text
src/                 前端页面
skills/              单据图片生成 skill
server.js            后端 API 与静态资源服务
start.sh             Linux/macOS 一键启动脚本
create_file/         生成图片保存目录，已加入 git 忽略
.runtime/            skill 解压运行目录，已加入 git 忽略
app_config.json      本地配置文件，已加入 git 忽略
```

## 本地启动

先创建 `app_config.json`，填写 MySQL 和推送配置。不要提交该文件。

示例结构：

```json
{
  "mysql": {
    "host": "127.0.0.1",
    "port": 3306,
    "database": "your_database",
    "user": "your_user",
    "password": "your_password"
  },
  "wecom": {
    "webhook": ""
  }
}
```

启动：

```bash
./start.sh
```

默认端口：

```text
8764
```

## 部署说明

服务器需要具备：

- Node.js 18+
- npm
- MySQL
- Chrome 或 Chromium
- WebP 转换工具之一：
  - `cwebp`
  - `ImageMagick`
  - `ffmpeg`

推荐安装：

```bash
sudo apt update
sudo apt install -y chromium-browser webp
```

如果以 root 运行 Chromium，skill 已处理 `--no-sandbox` 参数。

## Nginx

生产环境建议：

- `root` 指向 `build`
- `/api/` 反向代理到 `127.0.0.1:8764`
- `/create-file/` 或历史图片访问由后端 API 提供

## 后续规划

- 将 `中台.xlsx` 中的核心子表逐步落库。
- 建立订单结算主表，打通定金单、尾款单、对账单。
- 建立客户、车辆、押金、收入、成本、费用、审批等模块。
- 增加按角色展示不同菜单和数据权限。
- 增加查询、筛选、导出、统计看板。
- 后续可对接企业微信智能表格或企业微信应用接口，实现数据双向同步。

## 接入现有网站计划

目标：把当前单据生成工具逐步升级为现有网站中的完整财务中台，先保证当前定金单、尾款单、对账单稳定运行，再一层一层接入订单、车辆、客户、财务流水和经营分析。

### 第 1 阶段：稳定当前系统

- 确认现有域名、Nginx、Node 服务、MySQL、图片生成环境稳定。
- 增加健康检查接口：
  - `GET /api/health`
  - 用于检查前端构建目录、配置文件、MySQL 配置、企业微信配置状态、三类 skill、图片目录、Chrome、WebP 转换工具。
  - 企业微信 webhook 未配置时会显示 `configured: false`，但不影响基础服务健康状态。
- 固定访问入口：用户访问财务域名时默认进入登录页。
- 完成基础账号体系：
  - 管理员账号
  - 工作人员账号
  - 账号启用/停用
  - 权限字段 `permission`
- 保留并稳定当前页面：
  - 首页
  - 定金单
  - 尾款单
  - 对账单
  - 历史图片
  - 推送配置
  - 账号管理
- 确认三类图片生成路径统一：
  - `create_file/djd/日期/`
  - `create_file/wkd/日期/`
  - `create_file/dzd/日期/`
- 确认三类图片命名统一：
  - `djd_订单编号_日期.webp`
  - `wkd_订单编号_日期.webp`
  - `dzd_订单编号_日期.webp`
- 启动脚本执行时自动创建图片目录：
  - `create_file/djd`
  - `create_file/wkd`
  - `create_file/dzd`
  - `create_file/_tmp`
- 后端服务启动时也会自动补齐上述目录，避免直接运行 `node server.js` 时目录缺失。
- 启动脚本执行时自动清理 skill runtime，确保更新后的 skill 会重新解压生效。
- 第一阶段验收方式：
  - 执行 `./start.sh` 能正常启动。
  - 访问财务域名默认进入登录页。
  - 登录后默认进入首页。
  - 管理员能看到推送和账号管理，工作人员不能看到。
  - 定金单、尾款单、对账单都能生成 WebP。
  - 生成文件进入对应目录。
  - 历史图片能按类型和日期查看。
  - 历史图片能预览、下载、推送。
  - 访问 `/api/health` 返回 `ok: true` 或明确指出缺少哪项环境。

### 第 2 阶段：整理数据库基础表

使用的数据库是：cardata_money
- 梳理当前已有表：
  - `cw_user`
  - `cw_djd`
  - `cw_wkd`
  - `cw_dzd`
- 根据 `中台.xlsx` 拆分核心业务表。
- 优先建立这些基础表：
  - 用户表
  - 客户表
  - 车辆表
  - 订单主表
  - 押金表
  - 收款流水表
  - 退款流水表
  - 成本费用表
  - 单据生成记录表
- 给所有业务表统一保留基础字段：
  - `id`
  - `create_time`
  - `create_user`
  - `update_time`
  - `status`

### 第 3 阶段：建立订单结算主表

- 以 `中台.xlsx` 中的 `订单结算主表（销售&车队长&财务）` 为核心蓝本。
- 先实现订单主数据：
  - 订单编号
  - 客户编号
  - 客户姓名
  - 联系方式
  - 车型
  - 车牌
  - 销售
  - 订单类型
  - 出车时间
  - 回车时间
  - 出车公里
  - 回车公里
  - 出车油量
  - 回车油量
  - 用车天数
- 再接入财务结算字段：
  - 已收租金
  - 车辆押金
  - 违章押金
  - 合计收款
  - 实际消费
  - 应退押金金额
  - 应补金额
  - 退款状态
  - 财务经手人

### 第 4 阶段：打通当前三类单据

- 定金单生成后写入订单主表或定金单记录表。
- 尾款单生成后写入订单主表或尾款单记录表。
- 对账单生成后写入订单结算主表。
- 三类单据都关联同一个 `order_id`。
- 历史图片中增加按订单编号查询。
- 订单详情页中展示该订单生成过的所有图片：
  - 定金单
  - 尾款单
  - 对账单
- 推送、下载、重新生成都写入操作记录。

### 第 5 阶段：接入客户与车辆模块

- 客户模块参考 `客户主档案（销售）`。
- 客户信息包括：
  - 客户编号
  - 姓名
  - 电话
  - 客户群
  - 客户类型
  - 会员等级
  - 来源
  - 负责销售
  - 成交记录
- 车辆模块参考：
  - `车型参数表（车队长）`
  - `在外车辆（包月）`
  - `长租车管理表`
  - `车辆异常ing（车队长）`
- 车辆信息包括：
  - 车辆 ID
  - 车牌号
  - 车型名称
  - 车辆分类
  - 车辆状态
  - 来源
  - 油箱容量
  - 推荐油号
  - 日租单价
  - 超公里单价
  - 限制公里/天

### 第 6 阶段：接入押金与资金流水

- 押金模块参考 `押金管理表`。
- 资金流水参考 `客户资金流水表（财务）`。
- 实现：
  - 车辆押金收取
  - 违章押金收取
  - 押金退还
  - 收款记录
  - 退款记录
  - 收款截图/退款截图
  - 客户欠款
  - 实收款统计
- 对账单中的应退金额、车辆押金、违章押金要能回写流水。

### 第 7 阶段：接入成本与费用模块

- 成本费用参考：
  - `报销费用明细（财务）`
  - `代驾费用明细表（财务）`
  - `调车成本表（车队长&财务）`
  - `板车费用明细表（车队长）`
  - `GPS付款`
  - `维修成本表(车队长&财务)`
  - `配件采购表（车队长&财务）`
- 先做通用费用录入。
- 再按费用类型拆分专属页面。
- 所有成本费用需要支持：
  - 付款日期
  - 费用类型
  - 金额
  - 关联订单
  - 关联车辆
  - 付款截图
  - 备注

### 第 8 阶段：接入审批与企业微信

- 推送能力保留企业微信 webhook。
- 后续增加企业微信应用接口能力：
  - 发送审批提醒
  - 发送收款提醒
  - 发送退款提醒
  - 发送回车结算提醒
  - 发送日报/周报
- 审批数据参考：
  - `同行付款申请`
  - `报销`
  - `审批明细（示例）`
- 如果后续接入企业微信智能表格，需要单独整理接口权限和字段映射。

### 第 9 阶段：数据查询与统计看板

- 首页从空页面升级为财务看板。
- 先做基础统计：
  - 今日收款
  - 今日退款
  - 今日生成单据数量
  - 今日出车数量
  - 今日回车数量
  - 未退押金数量
  - 待处理对账单数量
- 再做经营分析：
  - 月收入
  - 月成本
  - 月费用
  - 毛利润
  - 净利润
  - 车辆出租率
  - 单车利润
  - 销售业绩

### 第 10 阶段：权限、日志和安全

- 按角色控制菜单：
  - 管理员
  - 财务
  - 销售
  - 车队长
  - 普通工作人员
- 增加操作日志：
  - 登录
  - 新增
  - 修改
  - 删除/停用
  - 下载图片
  - 推送图片
  - 导出数据
- 重要数据禁止物理删除，统一使用 `status` 标记。
- 后续把明文密码改成加密存储。
- 增加数据库备份和图片文件备份。

### 推荐推进顺序

1. 稳定当前三类单据生成和推送。
2. 建立订单主表，把三类单据全部挂到订单编号下。
3. 做订单列表和订单详情。
4. 接入客户表和车辆表。
5. 接入押金和资金流水。
6. 接入成本费用。
7. 做首页财务看板。
8. 接入审批和企业微信更深层能力。
9. 完成权限、日志、备份和安全加固。
